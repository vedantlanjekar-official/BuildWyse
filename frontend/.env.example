# Supabase (optional — omit for memory-mode demo)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# FastAPI backend
VITE_API_URL=http://localhost:8000

# Enable demo login buttons (sends X-Dev-User-Id header; requires backend DEBUG=true)
VITE_DEV_AUTH=true
